# 项目规范性整理计划

## 📋 当前问题清单

### 需要移动的文件

#### 测试文件 → test/ 目录
- [ ] `test_plugin.js` → `test/`
- [ ] `test_plugin_simple.js` → `test/`
- [ ] `test_sha256.py` → `test/unit/`
- [ ] `test_xiaochen.wav` → `test/`
- [ ] `test_yunxi.wav` → `test/`
- [ ] `test/test_voice_extension.py` → `test/unit/`

#### 启动脚本 → scripts/ 目录
- [ ] `start_service.sh` → `scripts/`

#### 项目文档 → 合适位置
- [ ] `Azure配置指南.md` → 考虑是否保留或移动

### 需要删除的目录/文件
- [ ] `main/logs/` (与根目录 logs/ 重复)
- [ ] `old/.DS_Store` (无用的旧文件)

### 争议性内容
- [ ] `docs/koodo插件.md` (项目规范中未列出 docs/ 目录)
- [ ] `AGENTS.md`, `IFLOW.md`, `QWEN.md` (工具相关文档)

## 🎯 整理目标

根据 `project.md` 规范，根目录应保持：
```
azure-tts-test2/
├── main/          # 主要代码
├── test/          # 测试代码
├── logs/          # 日志文件
├── config/        # 配置文件
├── plugins/       # 插件文件
├── audio/         # 音频文件
├── scripts/       # 构建脚本
├── examples/      # 示例文件
├── .venv/         # 虚拟环境
├── .iflow/        # 工具目录
├── .qwen/         # 工具目录
├── openspec/      # 工具目录
└── 项目配置文件
```

## ⚠️ 注意
整理时需要：
1. 更新相关引用路径
2. 确保文件功能不受影响
3. 更新文档中的路径说明